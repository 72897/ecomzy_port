import React from "react";
import "./Spinner.css";
const Spinner = () => {
  return (
    <div className="spinner flex flex-col items-center justify-center text-center"></div>
  );
};

export default Spinner;
